# Core UI
PySide6==6.11.0

# Ollama client (field extraction via qwen3:4b)
ollama==0.6.1

# Image handling
pillow==12.2.0
pillow-heif==1.3.0

# PDF rendering
PyMuPDF==1.27.2.3

# Markdown rendering
Markdown==3.10.2

# OCR engine — PaddleOCR-VL v1.5 document parser
paddleocr[doc-parser]==3.6.0
# paddlex[ocr] is pulled in automatically by paddleocr[doc-parser]
# paddlepaddle: install GPU build from official index (auto-detects GPU at runtime):
#   pip install paddlepaddle-gpu==3.2.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# CPU fallback:
#   pip install paddlepaddle==3.2.1 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/

# Vietnamese OCR text correction (protonx-models/protonx-legal-tc)
# transformers 4.x required — 5.x breaks sentencepiece tokenizer for ViT5
transformers==4.47.1
sentencepiece==0.2.1
# torch CPU-only — install separately with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Version installed: torch==2.12.0+cpu
